enum TextSizes {
  small,
  medium,
  large,
}

enum Gender {
  male,
  female,
}

enum availablityStatus {
  Available,
  NotAvailable,
}

enum Role {
  consultant,
  user,
}

enum authStatus {
  authenticated,
  unauthenticated,
  notAuthenticated,
}
// enum: ['Available', 'Not Available'],
